<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kalkulator Serbaguna</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
      width: 350px;
      padding: 20px;
    }

    /* Tab Navigasi */
    .tabs {
      display: flex;
      justify-content: space-between;
      margin-bottom: 15px;
    }

    .tabs button {
      flex: 1;
      padding: 10px;
      border: none;
      background: #ddd;
      cursor: pointer;
      border-radius: 8px;
      margin: 0 5px;
      font-weight: bold;
    }

    .tabs button.active {
      background: #007bff;
      color: #fff;
    }

    .tab-content {
      display: none;
    }

    .tab-content.active {
      display: block;
    }

    /* Kalkulator Biasa */
    .calculator {
      background-color: #333;
      border-radius: 10px;
      padding: 15px;
    }

    #result {
      width: 100%;
      height: 60px;
      font-size: 24px;
      text-align: right;
      padding: 10px;
      margin-bottom: 10px;
      border: none;
      border-radius: 5px;
      background-color: #eee;
    }

    .buttons {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 10px;
    }

    button.calc-btn {
      padding: 20px;
      font-size: 20px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      background-color: #555;
      color: white;
    }

    button.calc-btn:hover {
      background-color: #777;
    }

    button.calc-btn:active {
      background-color: #888;
    }

    .equal {
      grid-row: span 2;
      background-color: #ff9500;
    }

    .equal:hover {
      background-color: #ffb143;
    }

    .zero {
      grid-column: span 2;
    }

    /* Kalkulator Diskon */
    .discount label {
      display: block;
      margin: 10px 0 5px;
      font-weight: bold;
    }

    .discount input {
      width: 100%;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      margin-bottom: 10px;
      font-size: 14px;
    }

    .discount button {
      width: 100%;
      padding: 10px;
      background: #007bff;
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
    }

    .discount button:hover {
      background: #0056b3;
    }

    .result {
      margin-top: 10px;
      padding: 10px;
      background: #f0f8ff;
      border-left: 5px solid #007bff;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Tabs -->
    <div class="tabs">
      <button class="tab-btn active" onclick="showTab('calc', event)">Kalkulator</button>
      <button class="tab-btn" onclick="showTab('diskon', event)">Diskon</button>
    </div>

    <!-- Kalkulator Biasa -->
    <div id="calc" class="tab-content active">
      <div class="calculator">
        <input type="text" id="result" disabled />
        <div class="buttons">
          <button class="calc-btn" onclick="clearDisplay()">C</button>
          <button class="calc-btn" onclick="backspace()">←</button>
          <button class="calc-btn" onclick="appendValue('/')">÷</button>
          <button class="calc-btn" onclick="appendValue('*')">×</button>

          <button class="calc-btn" onclick="appendValue('7')">7</button>
          <button class="calc-btn" onclick="appendValue('8')">8</button>
          <button class="calc-btn" onclick="appendValue('9')">9</button>
          <button class="calc-btn" onclick="appendValue('-')">−</button>

          <button class="calc-btn" onclick="appendValue('4')">4</button>
          <button class="calc-btn" onclick="appendValue('5')">5</button>
          <button class="calc-btn" onclick="appendValue('6')">6</button>
          <button class="calc-btn" onclick="appendValue('+')">+</button>

          <button class="calc-btn" onclick="appendValue('1')">1</button>
          <button class="calc-btn" onclick="appendValue('2')">2</button>
          <button class="calc-btn" onclick="appendValue('3')">3</button>
          <button class="calc-btn equal" onclick="calculate()">=</button>

          <button class="calc-btn zero" onclick="appendValue('0')">0</button>
          <button class="calc-btn" onclick="appendValue('.')">.</button>
        </div>
      </div>
    </div>

    <!-- Kalkulator Diskon -->
    <div id="diskon" class="tab-content">
      <div class="discount">
        <label for="harga">Harga Asli (Rp)</label>
        <input type="number" id="harga" placeholder="Masukkan harga asli">

        <label for="diskonInput">Diskon (%)</label>
        <input type="number" id="diskonInput" placeholder="Masukkan persen diskon">

        <button onclick="hitungDiskon()">Hitung</button>
        <div class="result" id="hasil"></div>
      </div>
    </div>
  </div>

  <script>
    // Fungsi format Rupiah
    function formatRupiah(angka) {
      return new Intl.NumberFormat('id-ID', {
        style: 'currency',
        currency: 'IDR'
      }).format(angka);
    }

    // Switch tab
    function showTab(tabId, e) {
      document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
      document.querySelectorAll('.tab-content').forEach(tab => tab.classList.remove('active'));

      e.target.classList.add('active');
      document.getElementById(tabId).classList.add('active');
    }

    // Kalkulator Biasa
    function appendValue(value) {
      document.getElementById("result").value += value;
    }

    function clearDisplay() {
      document.getElementById("result").value = "";
    }

    function backspace() {
      let currentValue = document.getElementById("result").value;
      document.getElementById("result").value = currentValue.slice(0, -1);
    }

    function calculate() {
      let expression = document.getElementById("result").value;
      if (expression === "") {
        alert("Masukkan angka terlebih dahulu!");
        return;
      }
      if (expression.includes("/0")) {
        document.getElementById("result").value = "Error";
        return;
      }
      try {
        let result = eval(expression);
        document.getElementById("result").value = result;
      } catch (error) {
        document.getElementById("result").value = "Error";
      }
    }

    // Kalkulator Diskon
    function hitungDiskon() {
      let harga = document.getElementById("harga").value;
      let diskon = document.getElementById("diskonInput").value;
      let hasilDiv = document.getElementById("hasil");

      if(harga === "" || diskon === "") {
        hasilDiv.innerHTML = "<p style='color:red'>Harap isi semua input!</p>";
        return;
      }

      harga = parseFloat(harga);
      diskon = parseFloat(diskon);

      let potongan = (harga * diskon) / 100;
      let hargaAkhir = harga - potongan;

      hasilDiv.innerHTML = `
        <p>Harga Asli: ${formatRupiah(harga)}</p>
        <p>Potongan (${diskon}%): ${formatRupiah(potongan)}</p>
        <hr>
        <p><b>Harga Setelah Diskon: ${formatRupiah(hargaAkhir)}</b></p>
      `;
    }
  </script>
</body>
</html>
